// Bloque: Es un conjunto de instrucciones agrupadas entre llaves { }
// Sirve para organizar el código y definir qué pertenece a qué estructura (por ejemplo, a un if, a un for, a una función, etc.).
edad= 18;

if (edad > 18) {
    console.log("Sos mayor de edad");
}

else if (edad < 18) {
    console.log("sos menor de edad")
}

else {
    console.log("tenes justo 18 años")
}